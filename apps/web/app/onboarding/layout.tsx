import type { ReactNode } from "react";

/** Sin sidebar ni topbar — experiencia a pantalla completa. */
export default function OnboardingLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
