/** Bucket privado de capturas de wins. Se accede por signed URL. */
export const CLIENT_WINS_BUCKET = "client-wins";

/** TTL de los links firmados de las capturas. Una hora alcanza para verlas. */
export const WIN_SIGNED_URL_TTL_SECONDS = 60 * 60;
