"use client";

import type { ContextDocument } from "@/types/business-context";

/** Panel oculto hasta tener generación real de tareas desde transcripciones. */
export function SuggestedCallTasksPanel({
  document: _document,
}: {
  document: ContextDocument;
}) {
  return null;
}
